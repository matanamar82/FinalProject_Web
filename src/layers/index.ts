import { AnyLayer } from "mapbox-gl";
import { entitySources } from "../MapSource";

// const layers = (): AnyLayer[] => {
//     return entitySources()
//         .map(src => src.layers)
//         .flat(1);
// };

// const getLayerSource = (layerId: string) => {
//     const layerSource = entitySources()
// }
import { Button, Dialog, DialogActions, DialogContentText } from '@mui/material';
import axios from 'axios';
import { useEffect, useState } from 'react';
import { UseFetch } from 'use-http';

export function Heights({ShowHeights ,ShowHeightMod}:any)
{

 
    return(
        <div>

        </div>
        //<p>{data[0].elevation}</p>
        // <Dialog open={ShowHeights}>
        //     <DialogContentText>
        //         Elevation: {elevation} meters
        //     </DialogContentText>
        //     <DialogActions>
        //         <Button onClick={ShowHeightMod}>close</Button>
        //         <Button onClick={getElevation}>Show height</Button>
        //     </DialogActions>
        // </Dialog>
    )
}